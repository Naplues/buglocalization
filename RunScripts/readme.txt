1.This archieve contain the scripts to run our approach in the experimental data. 
2.Java 1.7 is required to run the software. 
3. If your OS is Linux, you can run  the scripts in the LinuxRunScripts.zip. 
If your OS is Windows, you can run the scripts in the WinRunScripts.zip
4. Please put the running scripts in the same directory of the tool and the experimental data.

